-- UMUTxMAMİ | STREET LIFE REMASTERED ULTRA RAGE V3
-- Hitbox Kaldırıldı / Tek Atma & Ayarlanabilir FOV Eklendi
-- Optimized for Xeno Executor (Mobile)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- Dinamik Ayarlar
local Config = {
    AimFOV = 150,             -- Başlangıç FOV Boyutu
    SpeedValue = 45,          -- Hızlı Rage Koşma
    OneHitMultiplier = 9999,  -- Tek atma hasar çarpanı
    FarmDelay = 0.3
}

local States = {
    Aimbot = false,
    SilentAim = false,
    ShowFOV = false,
    OneHit = false,           -- Tek Atma Durumu
    ESP = false,
    Speed = false,
    AutoFarm = false
}

------------------------------------------------------------------------
-- DİNAMİK FOV ÇEMBERİ (Xeno Drawing API)
------------------------------------------------------------------------
local FOVCircle = Drawing.new("Circle")
FOVCircle.Color = Color3.fromRGB(255, 0, 0)
FOVCircle.Thickness = 1.5
FOVCircle.Filled = false
FOVCircle.Transparency = 0.8

RunService.RenderStepped:Connect(function()
    if States.ShowFOV then
        FOVCircle.Visible = true
        FOVCircle.Radius = Config.AimFOV
        FOVCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    else
        FOVCircle.Visible = false
    end
end)

------------------------------------------------------------------------
-- UI OLUŞTURMA (Mobil Kırmızı Premium UI)
------------------------------------------------------------------------
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "UMUTxMAMIRageMenuV3"
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 260, 0, 360)
MainFrame.Position = UDim2.new(0.5, -130, 0.5, -180)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true

local MainCorner = Instance.new("UICorner", MainFrame)
MainCorner.CornerRadius = UDim.new(0, 10)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 45)
Title.BackgroundColor3 = Color3.fromRGB(40, 0, 0)
Title.Text = "UMUTxMAMİ | ULTRA RAGE V3"
Title.TextColor3 = Color3.fromRGB(255, 0, 0)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 15
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 10)

-- AÇ/KAPAT BUTONU
local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Size = UDim2.new(0, 60, 0, 45)
ToggleBtn.Position = UDim2.new(0, 15, 0.45, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(220, 0, 0)
ToggleBtn.Text = "MENU"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 12
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 8)
ToggleBtn.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)

local Scroll = Instance.new("ScrollingFrame", MainFrame)
Scroll.Size = UDim2.new(1, -10, 1, -55)
Scroll.Position = UDim2.new(0, 5, 0, 50)
Scroll.BackgroundTransparency = 1
Scroll.CanvasSize = UDim2.new(0, 0, 0, 380)
Scroll.ScrollBarThickness = 3

local UIListLayout = Instance.new("UIListLayout", Scroll)
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 8)
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- Buton Yapıcı Fonksiyon
local function CreateFeatureButton(displayName, stateKey)
    local btn = Instance.new("TextButton", Scroll)
    btn.Size = UDim2.new(0.95, 0, 0, 38)
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    btn.Text = displayName .. ": KAPALI"
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.Font = Enum.Font.GothamSemibold
    btn.TextSize = 12
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

    btn.MouseButton1Click:Connect(function()
        States[stateKey] = not States[stateKey]
        if States[stateKey] then
            btn.BackgroundColor3 = Color3.fromRGB(160, 0, 0)
            btn.Text = displayName .. ": AKTİF"
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        else
            btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
            btn.Text = displayName .. ": KAPALI"
            btn.TextColor3 = Color3.fromRGB(200, 200, 200)
        end
    end)
end

-- Tıkladıkça Büyüyen FOV Ayarlayıcı
local function CreateFOVAdjuster()
    local btn = Instance.new("TextButton", Scroll)
    btn.Size = UDim2.new(0.95, 0, 0, 38)
    btn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    btn.Text = "FOV Ayarı: " .. Config.AimFOV
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamSemibold
    btn.TextSize = 12
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

    btn.MouseButton1Click:Connect(function()
        if Config.AimFOV >= 350 then
            Config.AimFOV = 100
        else
            Config.AimFOV = Config.AimFOV + 50
        end
        btn.Text = "FOV Ayarı: " .. Config.AimFOV
    end)
end

-- Menü Butonları Dizilimi
CreateFeatureButton("Aimbot (Kilitlenme)", "Aimbot")
CreateFeatureButton("Silent Aim (Mıknatıs)", "SilentAim")
CreateFeatureButton("FOV Çemberini Göster", "ShowFOV")
CreateFOVAdjuster()
CreateFeatureButton("1-Hit Damage (Tek Atma)", "OneHit")
CreateFeatureButton("Oyuncu ESP", "ESP")
CreateFeatureButton("Rage Speed (Hızlı Koşma)", "Speed")
CreateFeatureButton("Legit Auto Farm", "AutoFarm")

------------------------------------------------------------------------
-- CORE FUNCTIONS (HACK ENGINE)
------------------------------------------------------------------------

local function GetClosestTarget()
    local target = nil
    local shortestDistance = math.huge
    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer and v.Character and v.Character:FindFirstChild("Head") and v.Character:FindFirstChild("Humanoid") and v.Character.Humanoid.Health > 0 then
            local pos, onScreen = Camera:WorldToViewportPoint(v.Character.Head.Position)
            if onScreen then
                local dist = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)).Magnitude
                if dist < Config.AimFOV and dist < shortestDistance then
                    target = v
                    shortestDistance = dist
                end
            end
        end
    end
    return target
end

-- AIMBOT LOOP
RunService.RenderStepped:Connect(function()
    if States.Aimbot then
        local target = GetClosestTarget()
        if target and target.Character and target.Character:FindFirstChild("Head") then
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, target.Character.Head.Position)
        end
    end
end)

-- SILENT AIM & ONE-HIT DAMAGE INJECTION (Metatable Hook)
local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
local oldIndex = mt.__index
setreadonly(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    -- Silent Aim Tetikleyici
    if States.SilentAim and (method == "FindPartOnRayWithIgnoreList" or method == "FindPartOnRay") then
        local target = GetClosestTarget()
        if target and target.Character and target.Character:FindFirstChild("Head") then
            return target.Character.Head, target.Character.Head.Position, Vector3.new(0,1,0), target.Character.Head.Material
        end
    end
    
    -- Tek Atma Mantığı: Silah hasar verilerini sunucuya atan remote eventleri yakalar
    if States.OneHit and method == "FireServer" and tostring(self):lower():find("damage") then
        for i, arg in pairs(args) do
            if type(arg) == "number" then
                args[i] = Config.OneHitMultiplier -- Hasarı 9999 olarak değiştirir
            end
        end
        return oldNamecall(self, unpack(args))
    end
    
    return oldNamecall(self, ...)
end)

-- Silah değerlerini hafızadan direkt manipüle etmek için alternatif tarama (One-Hit için)
mt.__index = newcclosure(function(self, key)
    if States.OneHit and (key == "Damage" or key == "HitDamage") and self:IsA("NumberValue") then
        return Config.OneHitMultiplier
    end
    return oldIndex(self, key)
end)

setreadonly(mt, true)

-- ESP LOOP
task.spawn(function()
    while true do
        task.wait(1)
        if States.ESP then
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                    if not p.Character:FindFirstChild("MamiESP") then
                        local highlight = Instance.new("Highlight", p.Character)
                        highlight.Name = "MamiESP"
                        highlight.FillColor = Color3.fromRGB(255, 0, 0)
                        highlight.FillTransparency = 0.5
                    end
                end
            end
        else
            for _, p in pairs(Players:GetPlayers()) do
                if p.Character and p.Character:FindFirstChild("MamiESP") then p.Character.MamiESP:Destroy() end
            end
        end
    end
end)

-- RAGE SPEED LOOP
RunService.Heartbeat:Connect(function()
    if States.Speed and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = Config.SpeedValue
    elseif not States.Speed and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        if LocalPlayer.Character.Humanoid.WalkSpeed == Config.SpeedValue then
            LocalPlayer.Character.Humanoid.WalkSpeed = 16
        end
    end
end)

-- AUTO FARM LOOP
task.spawn(function()
    while true do
        task.wait(Config.FarmDelay)
        if States.AutoFarm and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            for _, obj in pairs(workspace:GetDescendants()) do
                if not States.AutoFarm then break end
                if obj:IsA("ProximityPrompt") then
                    if (LocalPlayer.Character.HumanoidRootPart.Position - obj.Parent.Position).Magnitude < 20 then
                        fireproximityprompt(obj)
                    end
                end
            end
        end
    end
end)

print("UMUTxMAMİ Ultra Rage V3 Successfully Injected on Xeno!")
