local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Layroxc Hub | Street Life",
   LoadingTitle = "Street Life Remastered",
   LoadingSubtitle = "by Layroxc",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "LayroxcScripts"
   }
})

-- Değişkenler
local AutoFarm = false
local HitboxEnabled = false
local HitboxSize = 10

-- Anti-Kick Işınlanma Fonksiyonu (Tween)
local function safeTeleport(targetCFrame)
    local char = game.Players.LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        local tweenService = game:GetService("TweenService")
        local info = TweenInfo.new(1, Enum.EasingStyle.Linear) -- Hızı buradan ayarlayabilirsin
        local tween = tweenService:Create(char.HumanoidRootPart, info, {CFrame = targetCFrame})
        tween:Play()
        tween.Completed:Wait()
    end
end

-- TABS
local MainTab = Window:CreateTab("Auto Farm", 4483362458)
local CombatTab = Window:CreateTab("Combat", 4483362458)

-- AUTO FARM SECTION
MainTab:CreateToggle({
   Name = "Auto Oil Farm (Safe Mode)",
   CurrentValue = false,
   Callback = function(Value)
      AutoFarm = Value
      while AutoFarm do
         task.wait(1)
         for _, obj in pairs(game.Workspace:GetDescendants()) do
            if obj.Name == "Oil" or obj.Name == "OilBarrel" then
               -- Petrolün yanına güvenli git
               safeTeleport(obj.CFrame * CFrame.new(0, 5, 0))
               task.wait(0.5)
               -- Toplama (ProximityPrompt tetikleme)
               local prompt = obj:FindFirstChildOfClass("ProximityPrompt")
               if prompt then fireproximityprompt(prompt) end
               
               -- Saklan (Yer altına veya havaya)
               safeTeleport(CFrame.new(obj.Position.X, 100, obj.Position.Z))
               task.wait(2)
            end
            if not AutoFarm then break end
         end
      end
   end,
})

-- COMBAT SECTION
CombatTab:CreateSlider({
   Name = "Hitbox Size",
   Range = {10, 50},
   Increment = 1,
   Suffix = "Studs",
   CurrentValue = 10,
   Callback = function(Value)
      HitboxSize = Value
   end,
})

CombatTab:CreateToggle({
   Name = "Enable Hitbox (Headshot Focus)",
   CurrentValue = false,
   Callback = function(Value)
      HitboxEnabled = Value
      game:GetService("RunService").RenderStepped:Connect(function()
         if HitboxEnabled then
            for _, v in pairs(game.Players:GetPlayers()) do
               if v ~= game.Players.LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                  v.Character.HumanoidRootPart.Size = Vector3.new(HitboxSize, HitboxSize, HitboxSize)
                  v.Character.HumanoidRootPart.Transparency = 0.7
                  v.Character.HumanoidRootPart.CanCollide = false
               end
            end
         end
      end)
   end,
})

Rayfield:Notify({
   Title = "Sistem Hazır",
   Content = "Layroxc Menü Başarıyla Yüklendi!",
   Duration = 5,
   Image = 4483362458,
})
