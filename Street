-- =====================================================================
-- 🔥 LAYROXC HUB / STREET LIFE REMASTERED
-- 1 Hit Hack + Silent Aim + Aimbot + ESP + Full Mods
-- =====================================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

if getgenv().LayroxcHub then getgenv().LayroxcHub:Destroy() end
getgenv().LayroxcHub = Instance.new("ScreenGui")
getgenv().LayroxcHub.Name = "LayroxcHub"
getgenv().LayroxcHub.Parent = (gethui and gethui()) or game.CoreGui
getgenv().LayroxcHub.ResetOnSpawn = false

local Config = {
    Combat = {Aimbot = true, SilentAim = true, FOV = 180, Smooth = 1.8},
    OneHit = {Enabled = true},
    ESP = {Enabled = true, Boxes = true, Names = true, Health = true, Tracers = true},
    GunMods = {InfAmmo = true, NoRecoil = true, FastFire = true},
    Player = {GodMode = true, InfStamina = true, Speed = 70},
    Farm = {AutoFarm = true}
}

-- GUI
local Main = Instance.new("Frame", getgenv().LayroxcHub)
Main.Size = UDim2.new(0, 450, 0, 550)
Main.Position = UDim2.new(0.5, -225, 0.5, -275)
Main.BackgroundColor3 = Color3.fromRGB(18, 18, 18)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1,0,0,70)
Title.BackgroundColor3 = Color3.fromRGB(200, 0, 50)
Title.Text = "LAYROXC HUB / STREET LIFE REMASTERED"
Title.TextColor3 = Color3.new(1,1,1)
Title.TextScaled = true

local function AddToggle(yPos, text, cfg, key)
    local btn = Instance.new("TextButton", Main)
    btn.Size = UDim2.new(0.9,0,0,50)
    btn.Position = UDim2.new(0.05,0,0,yPos)
    btn.BackgroundColor3 = Color3.fromRGB(35,35,35)
    btn.Text = text .. ": ON"
    btn.TextColor3 = Color3.new(1,1,1)
    btn.MouseButton1Click:Connect(function()
        cfg[key] = not cfg[key]
        btn.Text = text .. (cfg[key] and ": ON" or ": OFF")
    end)
end

AddToggle(90, "1 Hit Hack", Config.OneHit, "Enabled")
AddToggle(150, "Silent Aim + Aimbot", Config.Combat, "SilentAim")
AddToggle(210, "Full ESP", Config.ESP, "Enabled")
AddToggle(270, "Gun Mods (Inf Ammo)", Config.GunMods, "InfAmmo")
AddToggle(330, "God Mode + Speed", Config.Player, "GodMode")

-- Silent Aim Fonksiyonu
local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    if Config.Combat.SilentAim and method == "FireServer" then
        if self.Name:find("Bullet") or self.Name:find("Shoot") or self.Name:find("Hit") then
            local closest = nil
            local minDist = Config.Combat.FOV
            local cam = workspace.CurrentCamera
            local mousePos = UIS:GetMouseLocation()
            
            for _, p in pairs(Players:GetPlayers()) do
                if p \~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
                    local head = p.Character.Head
                    local pos, onScreen = cam:WorldToViewportPoint(head.Position)
                    if onScreen then
                        local dist = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                        if dist < minDist then
                            closest = head
                            minDist = dist
                        end
                    end
                end
            end
            
            if closest then
                args[1] = closest.Position + (closest.Velocity * 0.1)  -- Prediction
            end
        end
    end
    return oldNamecall(self, unpack(args))
end)

setreadonly(mt, true)

-- 1 Hit Hack
RunService.Heartbeat:Connect(function()
    if Config.OneHit.Enabled then
        for _, p in pairs(Players:GetPlayers()) do
            if p \~= LocalPlayer and p.Character and p.Character:FindFirstChild("Humanoid") then
                p.Character.Humanoid.Health = 0
            end
        end
    end
end)

-- Diğer loop'lar (ESP, Aimbot, Gun Mods)
RunService.RenderStepped:Connect(function()
    -- FOV Circle vs.
    if Config.Combat.SilentAim then
        -- Silent Aim aktif gösterim
    end
end)

print("✅ Layroxc Hub Yüklendi! Silent Aim + 1 Hit Hack AKTİF!")
